apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: collect-results
  labels:
    app.kubernetes.io/part-of: konflux-certsuite
spec:
  description: |
    Collects certsuite results and pushes them to two destinations:
    1. cert-track-results web app via API (POST /api/v1/claim)
    2. OCI artifact registry as a tarball backup
  params:
    - name: CERT_TRACK_URL
      type: string
      description: Base URL of the cert-track-results instance
    - name: CERT_TRACK_SECRET_NAME
      type: string
      description: Secret containing the API token
    - name: CERT_TRACK_SECRET_KEY
      type: string
      default: "api-token"
    - name: RELEASE_VERSION
      type: string
      description: Release version for cert-track-results (e.g. "4.20")
    - name: OPERATOR_NAME
      type: string
      description: Operator name for cert-track-results
    - name: OCI_REF
      type: string
      description: OCI artifact reference (quay.io/org/repo:tag)
    - name: CREDENTIALS_SECRET_NAME
      type: string
      description: Secret with OCI registry credentials
  workspaces:
    - name: shared
      description: Shared workspace containing results/
  volumes:
    - name: oci-auth
      secret:
        secretName: $(params.CREDENTIALS_SECRET_NAME)
  steps:
    - name: push-to-cert-track
      image: quay.io/openshift/origin-cli:latest
      workingDir: $(workspaces.shared.path)
      env:
        - name: CERT_TRACK_URL
          value: "$(params.CERT_TRACK_URL)"
        - name: CERT_TRACK_SECRET_NAME
          value: "$(params.CERT_TRACK_SECRET_NAME)"
        - name: CERT_TRACK_SECRET_KEY
          value: "$(params.CERT_TRACK_SECRET_KEY)"
        - name: RELEASE_VERSION
          value: "$(params.RELEASE_VERSION)"
        - name: OPERATOR_NAME
          value: "$(params.OPERATOR_NAME)"
      script: |
        #!/usr/bin/env bash
        set -euo pipefail

        RESULTS_DIR="$(workspaces.shared.path)/results"

        API_TOKEN=$(kubectl get secret "${CERT_TRACK_SECRET_NAME}" \
          -o jsonpath="{.data.${CERT_TRACK_SECRET_KEY}}" | base64 -d)

        if [[ -z "${API_TOKEN}" ]]; then
          echo "WARNING: No API token found. Skipping cert-track upload."
          exit 0
        fi

        # Upload each suite's claim.json
        for CLAIM_FILE in "${RESULTS_DIR}"/*/claim.json; do
          [[ -f "${CLAIM_FILE}" ]] || continue
          SUITE_NAME=$(basename "$(dirname "${CLAIM_FILE}")")

          echo "[$(date -u +%FT%T.%3NZ)] Uploading ${SUITE_NAME}/claim.json to cert-track-results"

          HTTP_CODE=$(curl -s -o /tmp/response.json -w "%{http_code}" \
            -X POST "${CERT_TRACK_URL}/api/v1/claim" \
            -H "X-API-Token: ${API_TOKEN}" \
            -F "claim_file=@${CLAIM_FILE}" \
            -F "release_version=${RELEASE_VERSION}" \
            -F "operator_name=${OPERATOR_NAME}")

          if [[ "${HTTP_CODE}" -ge 200 && "${HTTP_CODE}" -lt 300 ]]; then
            echo "  Upload successful (HTTP ${HTTP_CODE})"
            cat /tmp/response.json
          else
            echo "  WARNING: Upload failed (HTTP ${HTTP_CODE})"
            cat /tmp/response.json || true
          fi
        done
    - name: push-to-oci
      image: quay.io/konflux-ci/konflux-test:v1.4.31@sha256:a7cae9e96663e277a3904d0c78630508ddb6cc8eebaa912a840bd20f68dcaad1
      workingDir: $(workspaces.shared.path)
      volumeMounts:
        - name: oci-auth
          mountPath: /auth
      env:
        - name: OCI_REF
          value: "$(params.OCI_REF)"
      script: |
        #!/usr/bin/env bash
        set -euo pipefail

        RESULTS_DIR="$(workspaces.shared.path)/results"

        if [[ ! -d "${RESULTS_DIR}" ]]; then
          echo "WARNING: No results directory found. Skipping OCI push."
          exit 0
        fi

        echo "[$(date -u +%FT%T.%3NZ)] Pushing results to OCI: ${OCI_REF}"

        mkdir -p /home/tool-box/.docker
        cp /auth/oci-storage-dockerconfigjson /home/tool-box/.docker/config.json 2>/dev/null || \
          cp /auth/.dockerconfigjson /home/tool-box/.docker/config.json 2>/dev/null || true

        cd "$(workspaces.shared.path)"
        tar czf /tmp/results.tar.gz results/

        oras push "${OCI_REF}" /tmp/results.tar.gz:application/gzip

        echo "[$(date -u +%FT%T.%3NZ)] OCI push complete"
